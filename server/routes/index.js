const auth = require("./auth");
const courses = require("./courses");
const notifications = require("./notifications");
const students = require("./students");

module.exports = { auth, courses, notifications, students };
